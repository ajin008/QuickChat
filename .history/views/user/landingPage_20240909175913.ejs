<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>QuickChat - Welcome</title>
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/public/images/icon-192x192.png"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        overflow: hidden;
      }

      .container {
        text-align: center;
        animation: fadeIn 1.5s ease-in-out;
      }

      .header {
        font-size: 3rem;
        font-weight: 700;
        color: #343a40;
        margin-bottom: 1rem;
        animation: slideInFromLeft 1.5s ease-in-out forwards;
      }

      .sub-header {
        font-size: 1.5rem;
        color: #6c757d;
        margin-bottom: 2rem;
        animation: slideInFromRight 1.5s ease-in-out forwards;
        animation-delay: 0.3s;
      }

      .btn-primary {
        background-color: #25d366;
        border-color: #25d366;
        padding: 0.75rem 1.5rem;
        font-size: 1.2rem;
        animation: fadeIn 1.5s ease-in-out forwards;
        animation-delay: 0.6s;
        transition: background-color 0.3s ease, transform 0.3s ease;
      }

      .btn-primary:hover {
        background-color: #1ebc59;
        border-color: #1ebc59;
        transform: translateY(-3px);
      }

      .animated-graphic {
        position: absolute;
        bottom: -100px;
        width: 100%;
        height: 200px;
        background: url("path/to/your/graphic.svg") no-repeat center bottom;
        background-size: cover;
        animation: slideUp 2s ease-in-out forwards;
        animation-delay: 1s;
      }

      @keyframes fadeIn {
        0% {
          opacity: 0;
        }
        100% {
          opacity: 1;
        }
      }

      @keyframes slideInFromLeft {
        0% {
          opacity: 0;
          transform: translateX(-50px);
        }
        100% {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInFromRight {
        0% {
          opacity: 0;
          transform: translateX(50px);
        }
        100% {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideUp {
        0% {
          transform: translateY(100%);
        }
        100% {
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">Welcome to QuickChat</div>
      <div class="sub-header">Connect with friends and family instantly</div>
      <a href="/user/signup" class="btn btn-primary">Get Started</a>
    </div>
    <div class="animated-graphic"></div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
